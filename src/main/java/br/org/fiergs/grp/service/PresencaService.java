package br.org.fiergs.grp.service;

import org.springframework.stereotype.Service;

@Service
public class PresencaService {

//    PresencaRepository repository;

    public void addPresenca(Long idReuniao, Long idDiretor) {

//        repository.addPresenca(idReuniao, idDiretor);

//        Logs logs = logsService.addLog(tipo, tabela, reuniao.getId(), detalhamento);
    }

}
